/**
 * The function returns sum of two numbers
 * @param firstNumber
 * @param secondNumber
 */
export const multiplyNumbers = (firstNumber: number, secondNumber: number): number => {
    return firstNumber * secondNumber
}
